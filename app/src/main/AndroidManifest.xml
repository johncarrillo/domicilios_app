<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.ufps.domicilios_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Domicilios_app"
        android:usesCleartextTraffic="true">
        <activity android:name=".DetallePedido"></activity>
        <activity android:name=".HistoryActivity" />
        <activity android:name=".ComentActivity" />
        <activity android:name=".DireccionActivity" />
        <activity android:name=".SalesCart" />
        <activity android:name=".MenuRestaurant" />
        <activity
            android:name=".IndexActivity"
            android:label="@string/title_activity_index"
            android:theme="@style/Theme.Domicilios_app.NoActionBar" />
        <activity android:name=".CreateUser" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>